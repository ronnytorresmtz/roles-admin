version: 0.0
os: linux
files:
  - source: /public
    destination: /var/www/html/roles-admin/public
  - source: /
    destination: /var/www/repo/roles-admin
hooks:
  BeforeInstall:
    - location: scripts/before_install.sh
      timeout: 300
      runas: root
    - location: scripts/start_server.sh
      timeout: 300
      runas: root
